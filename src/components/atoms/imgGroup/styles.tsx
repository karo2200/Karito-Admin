import { Box, styled } from '@mui/material';
export const userbox = styled(Box)({
	width: '100%',
	borderRadius: '50%',
	height: 32,
	position: 'absolute',
	marginLeft: 10,
});
export const usergroup = styled('img')({
	width: 32,
	borderRadius: '50%',
	height: 32,
	marginLeft: '-14px',
	objectFit: 'cover',
	display: 'inline',
});
export const Boxgroup = styled(Box)({
	width: '100%',
	position: 'relative',
});
